// Package entry point. Real exports (model registry, tool schemas) start in Milestone 5.

// Temporary proof that the workspace resolves this package straight from
// source with no build step. Removed once Milestone 5 adds real exports.
export const WORKSPACE_RESOLUTION_CHECK = "@twocode/shared is resolving via the workspace";
