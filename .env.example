# Local server URL the CLI talks to.
API_URL=http://localhost:3000

# --- Phase C: database ---
DATABASE_URL=

# --- Phase F: AI streaming (needed once we build real chat) ---
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# --- Phase H: Clerk auth (stubbed until then, real values needed later) ---
CLERK_FRONTEND_API=
CLERK_OAUTH_CLIENT_ID=
CLERK_OAUTH_CLIENT_SECRET=
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# --- Phase I: Polar billing (stubbed until then, real values needed later) ---
POLAR_ACCESS_TOKEN=
POLAR_PRODUCT_ID=
POLAR_SERVER=sandbox
POLAR_CREDITS_METER_ID=
